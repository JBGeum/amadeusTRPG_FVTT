# 무드 다이스 선택 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2개 이상 주사위를 굴린 판정에서 플레이어가 판정 다이스 1개와 무드 다이스 1개를 다이얼로그로 고르고, 선택 결과(판정 결과 + 무드 색)를 새 챗카드로 출력한다.

**Architecture:** 순수 로직(주사위 눈→색, 사용 가능 수, 자동 판정 인덱스, 결과 빌드)을 `module/dice/resolution.mjs`에 모아 Vitest로 검증한다. Foundry 의존부는 `module/mood/`에 격리한다 — 닫기 불가 AppV2 다이얼로그(`mood-dialog.mjs`)와 공통 진입 함수(`mood.mjs`의 `resolveMoodDice`). 두 굴림 진입점(`actor.rollAmadeAbl`, `item.getItemRollCard`)이 기존 굴림 카드 출력 후 `resolveMoodDice`를 호출한다.

**Tech Stack:** Foundry VTT v13 ApplicationV2 + HandlebarsApplicationMixin, Vitest, SCSS 토큰, Handlebars 템플릿.

**Spec:** `docs/superpowers/specs/2026-06-18-mood-dice-design.md`

---

## File Structure

- `module/dice/resolution.mjs` (수정) — 순수 함수 추가: `dieColor`, `colorToFace`, `usableCount`, `autoJudgeIndex`, `buildMoodResult`.
- `test/resolution.test.mjs` (수정) — 위 함수 테스트.
- `lang/ko.json` (수정) — `AMADEUS.mood.*` 라벨 추가.
- `module/helpers/templates.mjs` (수정) — `outcomeLabel` 헬퍼 등록 + `mood-result.html` 프리로드 등록.
- `templates/chatcard/mood-result.html` (신규) — 선택 결과 챗카드.
- `module/mood/mood-dialog.mjs` (신규) — 닫기 불가 AppV2 다이얼로그.
- `templates/dialog/mood-dialog.html` (신규) — 다이얼로그 본문.
- `module/mood/mood.mjs` (신규) — `resolveMoodDice` 공통 진입 함수.
- `module/documents/actor.mjs` (수정) — `rollAmadeAbl`에서 `resolveMoodDice` 호출.
- `module/documents/item.mjs` (수정) — `getItemRollCard`에서 `resolveMoodDice` 호출.
- `scss/components/_chatcard.scss` (수정) — 결과 카드 스타일.
- `scss/components/_dialog.scss` (수정) — 다이얼로그 무드 선택 스타일.

색 스와치는 매핑(black=1·red=2·blue=3·green=4·white=5)으로 기존 `.chat-die-chip--N`(die-face 색)을 재사용한다 — `colorToFace`가 이 매핑을 담당.

---

## Task 1: 순수 로직 (resolution.mjs)

**Files:**
- Modify: `module/dice/resolution.mjs`
- Test: `test/resolution.test.mjs`

- [ ] **Step 1: 실패 테스트 작성**

`test/resolution.test.mjs`의 import 구문에 새 함수를 추가한다(기존 import 블록 교체):

```javascript
import {
  diceCountForRank,
  resolveDie,
  rankVal,
  modVal,
  initHealth,
  initMoney,
  buildDiceset,
  dieColor,
  colorToFace,
  usableCount,
  autoJudgeIndex,
  buildMoodResult,
} from "../module/dice/resolution.mjs";
```

파일 끝에 다음 describe 블록들을 추가한다:

```javascript
describe("dieColor", () => {
  it("maps die faces to colors, 6 is special", () => {
    expect(dieColor(1)).toBe("black");
    expect(dieColor(2)).toBe("red");
    expect(dieColor(3)).toBe("blue");
    expect(dieColor(4)).toBe("green");
    expect(dieColor(5)).toBe("white");
    expect(dieColor(6)).toBe("special");
  });
});

describe("colorToFace", () => {
  it("maps the five real colors to die-face numbers for swatch reuse", () => {
    expect(colorToFace("black")).toBe(1);
    expect(colorToFace("red")).toBe(2);
    expect(colorToFace("blue")).toBe(3);
    expect(colorToFace("green")).toBe(4);
    expect(colorToFace("white")).toBe(5);
  });
  it("returns 0 for special/unknown", () => {
    expect(colorToFace("special")).toBe(0);
  });
});

describe("usableCount / autoJudgeIndex", () => {
  it("counts non-disabled dice (D-rank drops the higher one)", () => {
    expect(usableCount(buildDiceset([3, 5], "D"))).toBe(1); // 높은 5는 disabled
    expect(usableCount(buildDiceset([3], "C"))).toBe(1);
    expect(usableCount(buildDiceset([3, 5], "B"))).toBe(2);
    expect(usableCount(buildDiceset([1, 2, 6], "A"))).toBe(3);
  });
  it("finds the single usable index for auto cases", () => {
    expect(autoJudgeIndex(buildDiceset([3, 5], "D"))).toBe(0); // 낮은 3
    expect(autoJudgeIndex(buildDiceset([6, 2], "D"))).toBe(1); // 낮은 2
    expect(autoJudgeIndex(buildDiceset([4], "C"))).toBe(0);
  });
});

describe("buildMoodResult", () => {
  const base = { values: [4, 2, 6], modVal: 1, dc: 4 };
  it("builds judge with outcome and a colored mood die", () => {
    // judge = index0(4), mood = index1(2=red)
    const r = buildMoodResult({ ...base, judgeIndex: 0, moodIndex: 1, specialColor: null });
    expect(r.judge).toEqual({ value: 4, outcome: "success" }); // 4+1>=4
    expect(r.mood).toEqual({ value: 2, color: "red", face: 2, special: false });
  });
  it("uses the chosen specialColor when mood die is 6", () => {
    const r = buildMoodResult({ ...base, judgeIndex: 0, moodIndex: 2, specialColor: "green" });
    expect(r.mood).toEqual({ value: 6, color: "green", face: 4, special: true });
  });
  it("returns null mood for auto cases (moodIndex null)", () => {
    const r = buildMoodResult({ ...base, judgeIndex: 0, moodIndex: null, specialColor: null });
    expect(r.mood).toBeNull();
  });
  it("reports fumble/special outcomes on the judge die", () => {
    expect(buildMoodResult({ ...base, judgeIndex: 1, moodIndex: 0 }).judge.outcome).toBe("fail"); // 2+1<4
    const sp = buildMoodResult({ values: [6, 2], modVal: 0, dc: 4, judgeIndex: 0, moodIndex: 1 });
    expect(sp.judge.outcome).toBe("special");
    const fb = buildMoodResult({ values: [1, 2], modVal: 0, dc: 4, judgeIndex: 0, moodIndex: 1 });
    expect(fb.judge.outcome).toBe("fumble");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test -- --run`
Expected: 새 테스트들이 `dieColor is not a function` 등으로 FAIL.

- [ ] **Step 3: 최소 구현 추가**

`module/dice/resolution.mjs`의 `buildDiceset` 함수 바로 아래에 추가한다:

```javascript
const DIE_COLOR = { 1: "black", 2: "red", 3: "blue", 4: "green", 5: "white", 6: "special" };
const COLOR_FACE = { black: 1, red: 2, blue: 3, green: 4, white: 5 };

/** 주사위 눈 → 색 키. 6은 정식 색이 아니라 스페셜 표식. */
export function dieColor(value) {
  return DIE_COLOR[value] ?? null;
}

/** 색 키 → die-face 번호(1~5). 결과 카드의 색 스와치가 .chat-die-chip--N 색을 재사용하기 위함. */
export function colorToFace(color) {
  return COLOR_FACE[color] ?? 0;
}

/** buildDiceset 결과에서 사용 가능한(버려지지 않은) 주사위 수. 2 이상이면 무드 선택 다이얼로그. */
export function usableCount(diceset) {
  return diceset.filter((d) => !d.disabled).length;
}

/** 사용 가능 주사위가 1개뿐인 자동 케이스에서 그 인덱스. */
export function autoJudgeIndex(diceset) {
  return diceset.findIndex((d) => !d.disabled);
}

/**
 * 선택 결과 카드용 객체를 만든다.
 * @param {{values:number[], modVal:number, dc:number, judgeIndex:number, moodIndex:number|null, specialColor:string|null}} args
 * @returns {{judge:{value:number, outcome:string}, mood:null|{value:number, color:string, face:number, special:boolean}}}
 */
export function buildMoodResult({ values, modVal, dc, judgeIndex, moodIndex, specialColor }) {
  const judge = { value: values[judgeIndex], outcome: resolveDie(values[judgeIndex], modVal, dc) };
  let mood = null;
  if (moodIndex != null) {
    const value = values[moodIndex];
    const color = value === 6 ? specialColor : dieColor(value);
    mood = { value, color, face: colorToFace(color), special: value === 6 };
  }
  return { judge, mood };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test -- --run`
Expected: 전체 PASS (기존 26 + 신규). 경고 없음.

- [ ] **Step 5: 커밋**

```bash
git add module/dice/resolution.mjs test/resolution.test.mjs
git commit -m "feat: add pure mood-dice resolution helpers"
```

---

## Task 2: i18n 라벨 (lang/ko.json)

**Files:**
- Modify: `lang/ko.json`

- [ ] **Step 1: mood 라벨 블록 추가**

`lang/ko.json`에서 `"dice"` 블록(`"drop": "드롭"`으로 끝나고 `},`가 오는 부분) 바로 다음에 새 블록을 삽입한다. 즉 다음 코드의 앞부분과 일치시키고:

```json
    "dice" : {
      "special": "스페셜",
      "fumble": "펌블",
      "shift": "시프트",
      "1up": "1업",
      "2up": "2업",
      "1down": "1다운",
      "drop": "드롭"
    },
```

이 뒤에 아래를 추가한다:

```json
    "mood" : {
      "dialogTitle": "무드 다이스 선택",
      "hint": "판정 다이스와 무드 다이스를 하나씩 고르세요.",
      "judge": "판정 다이스",
      "mood": "무드 다이스",
      "pickColor": "스페셜 색 선택",
      "confirm": "확정",
      "resultTitle": "다이스 선택",
      "judgeLabel": "판정",
      "moodLabel": "무드",
      "noMood": "무드 없음"
    },
```

- [ ] **Step 2: JSON 유효성 확인**

Run: `node -e "JSON.parse(require('fs').readFileSync('lang/ko.json','utf8')); console.log('ok')"`
Expected: `ok` 출력 (구문 오류 없음).

- [ ] **Step 3: 커밋**

```bash
git add lang/ko.json
git commit -m "feat: add mood dice i18n labels"
```

---

## Task 3: 결과 챗카드 (헬퍼 + 템플릿 + 스타일 + 프리로드)

**Files:**
- Modify: `module/helpers/templates.mjs`
- Create: `templates/chatcard/mood-result.html`
- Modify: `scss/components/_chatcard.scss`

> 이 Task는 Foundry 렌더 의존이라 자동 테스트 대신 Step 5의 빌드 통과로 정적 검증하고, 외형은 핸드오프의 수동 검증 단계에서 확인한다.

- [ ] **Step 1: outcomeLabel 헬퍼 등록**

`module/helpers/templates.mjs`의 `successCheck` 헬퍼 등록 블록 바로 아래에 추가한다:

```javascript
  // 판정 결과 문자열 → 한국어 라벨. successCheck와 동일한 라벨 집합.
  Handlebars.registerHelper("outcomeLabel", function (outcome) {
    const label = { fumble: "펌블", special: "스페셜", success: "성공", fail: "실패" };
    return label[outcome] ?? "?";
  });
```

- [ ] **Step 2: 프리로드 목록에 결과 카드 추가**

`module/helpers/templates.mjs`의 `preloadHandlebarsTemplates` 배열에서 `"systems/amadeus/templates/chatcard/plot-result.html",` 줄 다음에 추가한다:

```javascript
    "systems/amadeus/templates/chatcard/mood-result.html",
```

- [ ] **Step 3: 결과 카드 템플릿 작성**

`templates/chatcard/mood-result.html` 생성:

```html
<div class="amadeus-chat chatcard mood-result-chatcard">
  <div class="chat-inner">
    <header class="chat-title">
      <i class="fa-solid fa-dice chat-title-icon"></i>
      <span class="chat-title-text">{{label}} · {{localize "AMADEUS.mood.resultTitle"}}</span>
    </header>
    <div class="chat-ornament" aria-hidden="true">
      <span class="chat-ornament-line chat-ornament-line--l"></span>
      <i class="fa-solid fa-diamond chat-ornament-dot"></i>
      <i class="fa-solid fa-dice-d20 chat-ornament-icon"></i>
      <i class="fa-solid fa-diamond chat-ornament-dot"></i>
      <span class="chat-ornament-line chat-ornament-line--r"></span>
    </div>

    <div class="mood-result-rows">
      <div class="mood-result-row">
        <span class="mood-result-tag">{{localize "AMADEUS.mood.judgeLabel"}}</span>
        <span class="chat-die-chip chat-die-chip--{{judge.value}}">
          <span class="chat-die-num lnum">{{judge.value}}</span>
        </span>
        <span class="mood-result-outcome">{{outcomeLabel judge.outcome}}</span>
      </div>

      <div class="mood-result-row">
        <span class="mood-result-tag">{{localize "AMADEUS.mood.moodLabel"}}</span>
        {{#if mood}}
        <span class="chat-die-chip chat-die-chip--{{mood.face}}">
          <span class="chat-die-num lnum">{{mood.value}}</span>
        </span>
        <span class="mood-result-color">{{localize (concat "AMADEUS.color." mood.color)}}</span>
        {{#if mood.special}}<span class="mood-result-badge">{{localize "AMADEUS.dice.special"}}</span>{{/if}}
        {{else}}
        <span class="mood-result-none">{{localize "AMADEUS.mood.noMood"}}</span>
        {{/if}}
      </div>
    </div>
  </div>
</div>
```

- [ ] **Step 4: 스타일 추가**

`scss/components/_chatcard.scss` 맨 끝에 추가한다:

```scss
// --------------------------------------------------------------------------
// Mood-result card (judge die + mood color)
// --------------------------------------------------------------------------
.amadeus-chat .mood-result-rows {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-top: 4px;
}

.amadeus-chat .mood-result-row {
  display: flex;
  align-items: center;
  gap: 8px;

  .chat-die-chip {
    width: 34px;
    height: 34px;
    border-radius: 7px;
  }
  .chat-die-num { font-size: 18px; }
}

.amadeus-chat .mood-result-tag {
  min-width: 38px;
  font-size: 12px;
  color: var(--pmuted);
}

.amadeus-chat .mood-result-outcome,
.amadeus-chat .mood-result-color {
  font-size: 14px;
  color: var(--ptext);
}

.amadeus-chat .mood-result-badge {
  font-size: 11px;
  padding: 1px 6px;
  border-radius: 9px;
  background: var(--d6);
  color: #4a3a12;
}

.amadeus-chat .mood-result-none {
  font-size: 13px;
  font-style: italic;
  color: var(--pmuted);
}
```

- [ ] **Step 5: 빌드 통과 확인**

Run: `npm run build`
Expected: `✓ built`, 에러 없음.

- [ ] **Step 6: 커밋**

```bash
git add module/helpers/templates.mjs templates/chatcard/mood-result.html scss/components/_chatcard.scss
git commit -m "feat: add mood-dice result chat card"
```

---

## Task 4: 선택 다이얼로그 (MoodDialog)

**Files:**
- Create: `module/mood/mood-dialog.mjs`
- Create: `templates/dialog/mood-dialog.html`
- Modify: `scss/components/_dialog.scss`

> Foundry AppV2 의존이라 자동 테스트 대신 빌드 통과(Step 4)로 정적 검증, 동작은 수동 검증.

- [ ] **Step 1: 다이얼로그 클래스 작성**

`module/mood/mood-dialog.mjs` 생성:

```javascript
import { FixedWidthMixin } from "../helpers/fixed-width.mjs";

const { ApplicationV2, HandlebarsApplicationMixin } = foundry.applications.api;

/**
 * 2개 이상 주사위 판정에서 판정 다이스 1개 + 무드 다이스 1개를 고르는 다이얼로그.
 * 닫기 불가(확정 필수). 무드 다이스가 6(스페셜)이면 흑적청녹백 중 색을 추가로 고른다.
 */
export class MoodDialog extends FixedWidthMixin(HandlebarsApplicationMixin(ApplicationV2)) {
  static DEFAULT_OPTIONS = {
    id: "amadeus-mood-dialog",
    classes: ["amadeus", "amadeus-dlg", "mood-dialog-app"],
    tag: "div",
    position: { width: 340, height: "auto" },
    window: { title: "AMADEUS.mood.dialogTitle", icon: "fa-solid fa-dice", resizable: true },
    actions: {
      pickJudge: MoodDialog.#onPickJudge,
      pickMood: MoodDialog.#onPickMood,
      pickColor: MoodDialog.#onPickColor,
      confirm: MoodDialog.#onConfirm,
    },
  };

  static PARTS = { main: { template: "systems/amadeus/templates/dialog/mood-dialog.html" } };

  constructor(options) {
    super(options);
    this.diceset = options.diceset;
    this.modVal = options.modVal;
    this.dc = options.dc;
    this.label = options.label;
    this._resolve = options.resolve;
    this.judgeIndex = null;
    this.moodIndex = null;
    this.specialColor = null;
    this._confirmed = false;
  }

  /** 닫기 불가: 확정 전에는 X/ESC를 무시한다. */
  async close(options = {}) {
    if (!this._confirmed && !options.force) return this;
    return super.close(options);
  }

  /** 강제 종료 시 대기 중인 Promise를 안전하게 해제. */
  _onClose(options) {
    super._onClose(options);
    if (!this._confirmed) this._resolve?.(null);
  }

  /** @override data-theme 주입 */
  _onRender(context, options) {
    super._onRender(context, options);
    this.element.dataset.theme = game.settings.get("amadeus", "theme");
  }

  async _prepareContext() {
    const moodValue = this.moodIndex != null ? this.diceset[this.moodIndex].value : null;
    const moodIsSpecial = moodValue === 6;
    const canConfirm =
      this.judgeIndex != null &&
      this.moodIndex != null &&
      (!moodIsSpecial || this.specialColor != null);
    return {
      label: this.label,
      modVal: this.modVal,
      dc: this.dc,
      dice: this.diceset.map((d, i) => ({
        index: i,
        value: d.value,
        isJudge: i === this.judgeIndex,
        isMood: i === this.moodIndex,
      })),
      moodIsSpecial,
      colors: ["black", "red", "blue", "green", "white"].map((key) => ({
        key,
        selected: key === this.specialColor,
      })),
      canConfirm,
    };
  }

  static #onPickJudge(event, target) {
    const i = Number(target.dataset.index);
    this.judgeIndex = i;
    if (this.moodIndex === i) this.moodIndex = null; // 같은 주사위 중복 금지
    this.render();
  }

  static #onPickMood(event, target) {
    const i = Number(target.dataset.index);
    this.moodIndex = i;
    if (this.judgeIndex === i) this.judgeIndex = null;
    if (this.diceset[i].value !== 6) this.specialColor = null; // 6 아니면 색 선택 무효화
    this.render();
  }

  static #onPickColor(event, target) {
    this.specialColor = target.dataset.color;
    this.render();
  }

  static #onConfirm() {
    const moodValue = this.diceset[this.moodIndex]?.value;
    const ok =
      this.judgeIndex != null &&
      this.moodIndex != null &&
      (moodValue !== 6 || this.specialColor != null);
    if (!ok) return;
    this._confirmed = true;
    this._resolve?.({
      judgeIndex: this.judgeIndex,
      moodIndex: this.moodIndex,
      specialColor: this.specialColor,
    });
    this.close();
  }

  /** 다이얼로그를 열고 선택 결과를 Promise로 반환한다. */
  static async prompt(context) {
    return new Promise((resolve) => {
      const dlg = new MoodDialog({ ...context, resolve });
      dlg.render(true);
    });
  }
}
```

- [ ] **Step 2: 다이얼로그 템플릿 작성**

`templates/dialog/mood-dialog.html` 생성:

```html
<div class="dlg-body mood-dialog-body">
  <p class="dlg-hint">{{localize "AMADEUS.mood.hint"}}</p>

  <div class="mood-die-list">
    {{#each dice as |d|}}
    <div class="mood-die-row">
      <span class="chat-die-chip chat-die-chip--{{d.value}}">
        <span class="chat-die-num lnum">{{d.value}}</span>
      </span>
      <span class="mood-die-outcome">{{successCheck d.value ../modVal ../dc}}</span>
      <button type="button" class="mood-pick-btn{{#if d.isJudge}} selected{{/if}}" data-action="pickJudge" data-index="{{d.index}}">{{localize "AMADEUS.mood.judge"}}</button>
      <button type="button" class="mood-pick-btn{{#if d.isMood}} selected{{/if}}" data-action="pickMood" data-index="{{d.index}}">{{localize "AMADEUS.mood.mood"}}</button>
    </div>
    {{/each}}
  </div>

  {{#if moodIsSpecial}}
  <div class="mood-color-block">
    <p class="dlg-hint-sub">{{localize "AMADEUS.mood.pickColor"}}</p>
    <div class="mood-color-row">
      {{#each colors as |c|}}
      <button type="button" class="mood-color-btn chat-die-chip chat-die-chip--{{colorFaceLit c.key}}{{#if c.selected}} selected{{/if}}" data-action="pickColor" data-color="{{c.key}}" title="{{localize (concat "AMADEUS.color." c.key)}}"></button>
      {{/each}}
    </div>
  </div>
  {{/if}}

  <div class="mood-confirm-row">
    <button type="button" class="dlg-btn mood-confirm" data-action="confirm" {{#unless canConfirm}}disabled{{/unless}}>{{localize "AMADEUS.mood.confirm"}}</button>
  </div>
</div>
```

이 템플릿은 `colorFaceLit` 헬퍼를 쓴다(색 키 → die-face 번호). `module/helpers/templates.mjs`의 `outcomeLabel` 헬퍼 등록 아래에 추가한다:

```javascript
  // 색 키 → die-face 번호. 색 스와치가 .chat-die-chip--N 색을 재사용하기 위함.
  Handlebars.registerHelper("colorFaceLit", function (color) {
    return colorToFace(color);
  });
```

그리고 같은 파일 상단 import에 `colorToFace`를 추가한다(기존 `import { resolveDie } from "../dice/resolution.mjs";`를 교체):

```javascript
import { resolveDie, colorToFace } from "../dice/resolution.mjs";
```

- [ ] **Step 3: 다이얼로그 스타일 추가**

`scss/components/_dialog.scss` 맨 끝에 추가한다:

```scss
// --------------------------------------------------------------------------
// Mood dice selection dialog
// --------------------------------------------------------------------------
.amadeus-dlg .mood-die-list {
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin: 8px 0;
}

.amadeus-dlg .mood-die-row {
  display: flex;
  align-items: center;
  gap: 8px;

  .chat-die-chip {
    width: 34px;
    height: 34px;
    border-radius: 7px;
    flex: 0 0 auto;
  }
  .chat-die-num { font-size: 18px; }
}

.amadeus-dlg .mood-die-outcome {
  min-width: 42px;
  font-size: 12px;
  color: var(--muted);
}

.amadeus-dlg .mood-pick-btn {
  flex: 1 1 0;
  padding: 4px 6px;
  font-size: 12px;
  border: 1px solid var(--line);
  border-radius: 6px;
  background: transparent;
  color: var(--text);
  cursor: pointer;

  &.selected {
    background: var(--gold);
    border-color: var(--gold);
    color: #1a1a1a;
  }
}

.amadeus-dlg .mood-color-block { margin: 6px 0; }

.amadeus-dlg .mood-color-row {
  display: flex;
  gap: 8px;
  justify-content: center;
}

.amadeus-dlg .mood-color-btn {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  border: 2px solid transparent;
  cursor: pointer;

  &.selected { border-color: var(--gold); }
}

.amadeus-dlg .mood-confirm-row {
  display: flex;
  justify-content: center;
  margin-top: 10px;
}

.amadeus-dlg .mood-confirm[disabled] {
  opacity: 0.45;
  cursor: not-allowed;
}
```

> 토큰: `.amadeus-dlg`는 `--bg/--panel/--panel2/--text/--muted/--line/--gold/--bar` + die-face `--d1..--d6`를 쓴다(`_dialog.scss` 상단 주석 기준). 확정 버튼은 기존 `.dlg-btn`(generic 버튼) 클래스를 재사용하므로 새 `.dlg-confirm`은 만들지 않고 `.mood-confirm`의 disabled 상태만 보강한다.

- [ ] **Step 4: 빌드 통과 확인**

Run: `npm run build`
Expected: `✓ built`, 에러 없음.

- [ ] **Step 5: 커밋**

```bash
git add module/mood/mood-dialog.mjs templates/dialog/mood-dialog.html module/helpers/templates.mjs scss/components/_dialog.scss
git commit -m "feat: add mood dice selection dialog"
```

---

## Task 5: 공통 진입 함수 + 굴림 진입점 연결

**Files:**
- Create: `module/mood/mood.mjs`
- Modify: `module/documents/actor.mjs`
- Modify: `module/documents/item.mjs`

> 통합부는 Foundry 런타임 의존이라 빌드/린트 통과(Step 4)로 정적 검증, 실제 흐름은 수동 검증.

- [ ] **Step 1: resolveMoodDice 작성**

`module/mood/mood.mjs` 생성:

```javascript
import { buildDiceset, usableCount, autoJudgeIndex, buildMoodResult } from "../dice/resolution.mjs";
import { postCard } from "../chat/chat.mjs";
import { MoodDialog } from "./mood-dialog.mjs";

/**
 * 굴림 직후 판정/무드 다이스를 확정하고 결과 카드를 출력한다.
 * 사용 가능 주사위가 1개(1d6 또는 D랭크)면 다이얼로그 없이 자동 확정, 2개 이상이면 다이얼로그.
 * @param {{actor:Actor, values:number[], rank:string, modVal:number, dc:number, label:string}} ctx
 */
export async function resolveMoodDice({ actor, values, rank, modVal, dc, label }) {
  const diceset = buildDiceset(values, rank);
  let judgeIndex;
  let moodIndex = null;
  let specialColor = null;

  if (usableCount(diceset) < 2) {
    judgeIndex = autoJudgeIndex(diceset);
  } else {
    const choice = await MoodDialog.prompt({ diceset, modVal, dc, label });
    if (!choice) return; // 강제 종료 안전장치
    ({ judgeIndex, moodIndex, specialColor } = choice);
  }

  const result = buildMoodResult({ values, modVal, dc, judgeIndex, moodIndex, specialColor });
  await postCard({
    actor,
    template: "systems/amadeus/templates/chatcard/mood-result.html",
    data: { label, ...result },
  });
}
```

- [ ] **Step 2: actor.rollAmadeAbl 연결**

`module/documents/actor.mjs` 상단 import에 추가(`buildDiceset` import 줄 아래):

```javascript
import { resolveMoodDice } from "../mood/mood.mjs";
```

`rollAmadeAbl`의 `await postCard({ ... });` 호출 바로 다음(메서드 닫는 `}` 직전)에 추가:

```javascript
    await resolveMoodDice({
      actor: this,
      values: roll.dice[0].values,
      rank,
      modVal: abl.modVal,
      dc: this.system.dc,
      label,
    });
```

- [ ] **Step 3: item.getItemRollCard 연결**

`module/documents/item.mjs` 상단 import에 추가(`buildDiceset` import 줄 아래):

```javascript
import { resolveMoodDice } from "../mood/mood.mjs";
```

`getItemRollCard`의 gift 분기에서 `await postCard({ ... });` 다음, `if (this.type === "gift") { ... }` 블록 안 끝에 추가:

```javascript
      await resolveMoodDice({
        actor,
        values: roll.dice[0].values,
        rank,
        modVal: actorAbl.modVal,
        dc: actor.system.dc,
        label,
      });
```

- [ ] **Step 4: 빌드 + 린트 + 테스트 통과 확인**

Run: `npm run build`
Expected: `✓ built`.

Run: `npm run lint`
Expected: 신규 error 0 (기존 baseline 외 추가 없음).

Run: `npm test -- --run`
Expected: 전체 PASS.

- [ ] **Step 5: 커밋**

```bash
git add module/mood/mood.mjs module/documents/actor.mjs module/documents/item.mjs
git commit -m "feat: wire mood dice selection into ability and gift rolls"
```

---

## Foundry 수동 검증 (구현 후)

`dist/`를 서버 `systems/amadeus`로 배포 후 브라우저 새로고침(매니페스트 미변경) 후:

- B/A/S 랭크 능력치 굴림 → 굴림 카드 출력 직후 무드 다이얼로그 자동 팝업.
- 판정+무드를 골라야 확정 버튼 활성, X/ESC로 닫히지 않음(닫기 불가).
- 무드로 6 선택 시 5색 선택 UI 노출, 색 골라야 확정 가능.
- 확정 후 결과 카드: 판정 다이스 색칩+성공/실패/펌블/스페셜, 무드 다이스 색칩+색이름(6이면 선택색+스페셜 배지).
- C랭크(1d6)·D랭크(2d6) → 다이얼로그 없이 결과 카드(무드 없음) 자동 출력.
- 기프트 굴림에서도 동일 동작.
- 멀티클라(GM+PL): 다이얼로그는 굴린 본인에게만, 결과 카드는 모두에게.
- 라/다 테마에서 다이얼로그 토큰 색 정상.

---

## Self-Review (작성자 체크 완료)

- **Spec 커버리지**: 룰 정의(눈→색·6스페셜·1펌블·자동 케이스) → Task 1·3·4 / 다이얼로그 닫기 불가 → Task 4 / 결과 카드 2개 흐름·자동도 카드 → Task 5 / 멀티 단방향(소켓 없음) → Task 5(`postCard`만). 누락 없음.
- **플레이스홀더**: 모든 코드 스텝에 실제 코드 포함. TBD/TODO 없음.
- **타입 일관성**: `buildMoodResult` 반환(`judge.value/outcome`, `mood.value/color/face/special`)이 결과 템플릿(Task 3)·`resolveMoodDice`(Task 5)와 일치. `MoodDialog.prompt` 반환(`judgeIndex/moodIndex/specialColor`)이 `resolveMoodDice` 구조 분해와 일치. `colorToFace`/`colorFaceLit` 일관.
- **주의**: Task 4 Step 3의 다이얼로그 토큰명(`--dtext` 등)은 실제 `_tokens.scss`와 대조해 보정할 것(주석 명시).
